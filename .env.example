# Copy this file to .env and fill in real values. The .env file is gitignored
# and must never be committed. These variables are consumed by docker-compose
# and by the bridges that reference ${VAR} placeholders in their config YAML.

# Sungrow inverter login password (referenced by sungrow2mqtt config as
# PASSWORD: ${SUNGROW_PASSWORD}).
SUNGROW_PASSWORD=
